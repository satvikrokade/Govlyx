# Copy this file to .env.production (or set in Netlify/Vercel dashboard)
# and fill in your backend URL.

# Backend API base URL — required in production (deployed builds).
# Leave empty or omit during local development (Vite proxy handles it).
VITE_API_URL=https://jan-sahayak-ai-84vh.onrender.com
